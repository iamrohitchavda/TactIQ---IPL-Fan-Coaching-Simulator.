# (No environment variables required)
# TactIQ runs fully offline — all scoring is done locally.
# The optional server/ folder can proxy a CricAPI key for live score tickers,
# but it is NOT needed to play the game.
